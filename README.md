# Celestial Janek (encrypted)

Password-protected build of an interactive birth-chart deck. The page is
AES-256 encrypted client-side (StatiCrypt); the source bytes here are ciphertext.
Enter the password at the gate to decrypt and view in your browser.
